# 60 Second Demo Script

Start on the dashboard and say: "This is the AI Secure Data Intelligence Platform. It acts as an AI Gateway, Data Scanner, Log Analyzer, and Risk Engine for security-sensitive content."

Paste the following into the inline analyzer:

```text
2026-03-10 INFO login
email=admin@company.com
password=admin123
api_key=sk-test-xyzABC12345
ERROR stack trace: null pointer
```

Then narrate:

1. "The platform detects exposed emails, passwords, API keys, and stack traces in real time."
2. "It assigns risk scores and recommends an action through the policy engine."
3. "Detected values are masked so analysts can safely review findings."
4. "AI insights summarize activity and explain why the risk matters."
5. "It also supports drag-and-drop multi-file upload for PDFs, DOCX, TXT, LOG, and SQL artifacts."

Close with:

"The full stack runs locally or in Docker, with a modular AI service compatible with OpenAI-style APIs."
